/**
 * components/admin/MetricCard.jsx — Admin Dashboard Metric Card
 *
 * Props: title, value, subtext, color, icon
 */
import React from 'react';
const MetricCard = ({ title, value, subtext, color, icon }) => null;
export default MetricCard;
