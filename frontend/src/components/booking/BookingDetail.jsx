/**
 * Full Booking Detail View
 *
 * Props: booking, role
 * Shows: job details, provider/customer info, payment breakdown, chat, action buttons
 * Embedded in /customer/bookings/:id and /provider/bookings/:id pages
 */
import React from 'react';
const BookingDetail = ({ booking, role }) => null;
export default BookingDetail;
