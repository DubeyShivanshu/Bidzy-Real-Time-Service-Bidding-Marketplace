/**
 * Responsive Data Table
 *
 * Props: columns (array of {key, label, render?}), data (array), loading, emptyMessage
 */
import React from 'react';
const Table = ({ columns, data, loading, emptyMessage }) => null;
export default Table;
