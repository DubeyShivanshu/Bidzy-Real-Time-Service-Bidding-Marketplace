/**
 * Side Navigation
 *
 * Props: role ('customer'|'provider'|'admin'), isOpen, onClose
 * Renders role-specific navigation links
 */
import React from 'react';
const Sidebar = ({ role, isOpen, onClose }) => null;
export default Sidebar;
