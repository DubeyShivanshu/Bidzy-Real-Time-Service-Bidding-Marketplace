/**
 * Verified Provider Badge
 *
 * Props: verified (boolean)
 * Shows checkmark badge if verified, pending badge otherwise
 */
import React from 'react';
const VerificationBadge = ({ verified }) => null;
export default VerificationBadge;
